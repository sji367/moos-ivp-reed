%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% LOG FILE:       ./MOOSLog_7_4_2017_____11_34_07/MOOSLog_7_4_2017_____11_34_07._moos
%% FILE OPENED ON  Wed Dec 31 19:00:00 1969
%% LOGSTART               1491579246.33
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
//-------------------------------------------------
// NAME: Sam Reed
// FILE: laptop.moos
// Intended for dry testing at Chase
//-------------------------------------------------

ServerHost = localhost
ServerPort = 9000
Community  = shoreside

//----  Next Line was commented and replaced with a command line overload ---- //
//MOOSTimeWarp = 1    (default)
MOOSTimeWarp = 1
TERM_REPORTING = true


// CCOM Pier
LatOrigin  = 43.071959194444446
LongOrigin = -70.711610833333339 

// Chase Tank
//LatOrigin  = 43.1358694
//LongOrigin = -70.9391542

//------------------------------------------
// Antler configuration  block
ProcessConfig = ANTLER
{
  MSBetweenLaunches = 400

  Run = MOOSDB          @ NewConsole = false
  Run = uSimMarine	@ NewConsole = false
  Run = pLogger         @ NewConsole = false
  //Run = pHelmIvP	@ NewConsole = false
  Run = pMarineViewer	  @ NewConsole = false
  Run = uProcessWatch	  @ NewConsole = false
  Run = pShare            @ NewConsole = false
  Run = pHostInfo       @ NewConsole = false
  Run = uFldShoreBroker  @ NewConsole = false
  Run = pPub_Points	@ NewConsole = true

}

//------------------------------------------
// Vehicle Communication Config
//------------------------------------------

ProcessConfig = pHostInfo                                       
{                                                               
  AppTick   = 8                                                 
  CommsTick = 8                                                 
                                                                
  TEMP_FILE_DIR  = ./                                           
  DEFAULT_HOSTIP = localhost    // default is "localhost"  
                                                                
  //DEFAULT_HOSTIP_FORCE = 192.168.0.55                           
} 

ProcessConfig = uFldShoreBroker                                 
{                                                               
  AppTick   = 8
  CommsTick = 8                  
                                                                
  //bridge = src=DEPLOY_ALL, alias=DEPLOY
  //bridge = src=DEPLOY_$V,  alias=DEPLOY
                                                                
  bridge = src=RETURN, alias=RETURN
  bridge = src=DESIRED_SPEED, alias=DESIRED_SPEED
  bridge = src=DESIRED_HEADING, alias=DESIRED_HEADING
  bridge = src=DEPLOY, alias=DEPLOY
  bridge = src=STATIONING, alias=STATIONING
  bridge = src=Obstacles, src=Obstacles
  bridge = src=simple, alias=simple
  bridge = src=ASTAR, alias=ASTAR
  bridge = src=NODE_REPORT_LOCAL, alias=NODE_REPORT
  bridge  = src=APPCAST_REQ, alias=APPCAST_REQ
  bridge = src=MOOS_MANUAL_OVERIDE, alias=MOOS_MANUAL_OVERIDE

                                                                
  // Note: [qbridge = FOO]  is shorthand for
  //       [bridge = src=FOO_$V,  alias=FOO] and
  //       [bridge = src=FOO_ALL, alias=FOO]
}                                                                          
                                                           
ProcessConfig = pShare
{
  AppTick   = 8
  CommsTick = 8

  input = route = localhost:9300
}

//------------------------------------------
// Other Config
//------------------------------------------

ProcessConfig = pPub_Points                                       
{                                                               
  AppTick   = 8                                                 
  CommsTick = 8                                                                      
}

//------------------------------------------
// pHelmIvP config block

ProcessConfig = pHelmIvP
{
  AppTick    = 4
  CommsTick  = 4

  term_report_interval=0

  bhv_dir_not_found_ok = true

  behaviors  = mission2.bhv
  domain     = course:0:359:360
  domain     = speed:0:4:21

  ok_skew    = any
}

//------------------------------------------
// uProcessWatch config block

ProcessConfig = uProcessWatch
{
  AppTick   = 4
  CommsTick = 4

  summary_wait = 5

  nowatch   = uMAC*
  nowatch   = uPokeDB*
  nowatch   = uTermCommand*
  watch_all = true
}


//------------------------------------------
// pLogger config block

ProcessConfig = pLogger
{
  AppTick   = 8
  CommsTick = 8

  AsyncLog = true

  // For variables that are published in a bundle on their first post,
  // explicitly declare their logging request

  Log = IVPHELM_LIFE_EVENT @ 0 NOSYNC

  LogAuxSrc = true
  WildCardLogging = true
  WildCardOmitPattern = *_STATUS
  WildCardOmitPattern = DB_VARSUMMARY
  WildCardOmitPattern = DB_RWSUMMARY
  WildCardExclusionLog = true
}

//------------------------------------------
// pMarineViewer config block

ProcessConfig = pMarineViewer
{
  AppTick    = 4
  CommsTick  = 4

  tiff_file            = data/portsmouth2.tif

  // Transit from UNH Pier
  set_pan_x            = 275
  set_pan_y            = -475
  zoom                 = 4.5

  vehicle_shape_scale  = 1.5//3

  trails_point_size   = 1

  // Appcast configuration
  appcast_height       = 75
  trails_p
  appcast_width        = 302
  appcast_viewable     = true
  appcast_color_scheme = indigo
  nodes_font_size      = medium
  procs_font_size      = medium
  appcast_font_size    = small

  //left_context[update_return] = VIEW_POINT = x=$(XPOS),y=$(YPOS),label=$(XPOS),$(YPOS)

  right_context[return] = DEPLOY=true
  right_context[return] = MOOS_MANUAL_OVERIDE=false
  right_context[return] = RETURN=false
  right_context[return] = STATIONING=false

  left_context[t_lvl1] = VIEW_POINT = x=$(XPOS),y=$(YPOS),label=1 x=$(XPOS) y=$(YPOS),vertex_size=5, vertex_color=greenyellow
  left_context[t_lvl1] = NEW_POINTS = x=$(XPOS),y=$(YPOS),1,Phantom

  left_context[t_lvl2] = VIEW_POINT = x=$(XPOS),y=$(YPOS),label=2 x=$(XPOS) y=$(YPOS),vertex_size=5, vertex_color=gold
  left_context[t_lvl2] = NEW_POINTS = x=$(XPOS),y=$(YPOS),2,Phantom

  left_context[t_lvl3] = VIEW_POINT = x=$(XPOS),y=$(YPOS),label=3 x=$(XPOS) y=$(YPOS),vertex_size=5, vertex_color=darkorange
  left_context[t_lvl3] = NEW_POINTS = x=$(XPOS),y=$(YPOS),3,Phantom

  left_context[t_lvl4] = VIEW_POINT = x=$(XPOS),y=$(YPOS),label=4 x=$(XPOS) y=$(YPOS),vertex_size=5, vertex_color=red
  left_context[t_lvl4] = NEW_POINTS = x=$(XPOS),y=$(YPOS),4,Phantom

  left_context[t_lvl5] = VIEW_POINT = x=$(XPOS),y=$(YPOS),label=5 x=$(XPOS) y=$(YPOS),vertex_size=5, vertex_color=black
  left_context[t_lvl5] = NEW_POINTS = x=$(XPOS),y=$(YPOS),5,Phantom

  button_one = DEPLOY # DEPLOY=true
  button_one = MOOS_MANUAL_OVERIDE=false # RETURN=false
  button_two = RETURN # RETURN=true
  button_three = CONTINUE # STATIONING=false # DEPLOY=true
  button_four = STATION_KEEP # STATIONING=true
  button_four = RETURN=false # DEPLOY=false
	
	SCOPE= Obstacles,Angles,Min,Current_Tide,ENCs, WPT_UPDATE, Poly_Obs, num_obs

  action  = MENU_KEY=deploy # DEPLOY = true # RETURN = false
  action+ = MENU_KEY=deploy # MOOS_MANUAL_OVERIDE=false
  action  = RETURN=true
  //action  = UPDATES_RETURN=speed=1.4

  point_viewable_labels = false
  polygon_viewable_labels = false
  seglist_viewable_labels = false
  vehicles_name_mode = names
  trails_viewable = false
	tiff_viewable = true
}

ProcessConfig = uSimMarine
{
  AppTick	= 4
  CommsTick	= 4

  start_x       = 0
  start_y       = 0

  start_heading = 180
  start_speed   = 0

  prefix        = NAV

  turn_rate     = 40
  thrust_map    = 0:0, 20:1, 40:2, 60:3, 80:4, 100:5
}


